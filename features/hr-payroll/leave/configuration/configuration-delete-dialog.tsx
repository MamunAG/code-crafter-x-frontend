"use client"

import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "@/components/ui/alert-dialog"
import type { MasterDataRecord } from "../../master-data/master-data.types"

export function ConfigurationDeleteDialog({ singular, confirm, submitting, onClose, onConfirm }: { singular: string; confirm: { action: "delete" | "restore"; record: MasterDataRecord } | null; submitting: boolean; onClose: () => void; onConfirm: () => void }) { return <AlertDialog open={Boolean(confirm)} onOpenChange={(open) => { if (!open) onClose() }}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>{confirm?.action === "restore" ? `Restore ${singular}?` : `Delete ${singular}?`}</AlertDialogTitle><AlertDialogDescription>{confirm?.action === "restore" ? `${confirm.record.name} will return to the active list.` : `${confirm?.record.name ?? `This ${singular}`} will be soft deleted. Existing history and assignments remain intact.`}</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel disabled={submitting}>Cancel</AlertDialogCancel><AlertDialogAction variant={confirm?.action === "delete" ? "destructive" : "default"} disabled={submitting} onClick={onConfirm}>{confirm?.action === "restore" ? "Restore" : "Delete"}</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog> }
